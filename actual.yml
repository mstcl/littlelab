---
- name: Install actual
  hosts: homelab
  gather_facts: false
  vars:
    service_directory: "actual"
    service_image: "actualbudget/actual-server"
    image_tag: "latest"
    url: "{{ urls.actual }}"

  roles:
    - role: urls
    - role: config
      vars:
        directories: [{}]
    - role: docker
    - role: traefik
      vars:
        servers:
          - port: 5006
        middlewares:
          - "internal-ip@file"
