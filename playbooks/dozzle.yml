---
- name: Install dozzle
  hosts: homelab
  gather_facts: false
  vars:
    service_directory: "dozzle"
    image: "amir20/dozzle:v6.2.3"
    network: "socket-net"
    url: "{{ urls.dozzle }}"

  roles:
    - role: urls
    - role: config
      vars:
        directories: [{}]
    - role: docker
    - role: traefik
      vars:
        servers: [{}]
        middlewares:
          - "authelia@file"
          - "internal-ip@file"
