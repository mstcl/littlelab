---
- name: Install readeck
  hosts: homelab
  gather_facts: false
  vars:
    service_directory: "readeck"
    image: "codeberg.org/readeck/readeck:latest"
    url: "{{ urls.readeck }}"

  roles:
    - role: urls
    - role: config
      vars:
        directories: [{}]
    - role: docker
    - role: traefik
      vars:
        servers: [{}]
        middlewares:
          - "internal-ip@file"
