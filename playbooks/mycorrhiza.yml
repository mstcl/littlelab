---
- name: Install mycorrhiza
  hosts: homelab
  gather_facts: false
  vars_files:
    - vars/mycorrhiza.yml

  roles:
    - role: urls
    - role: config
    - role: docker
    - role: traefik
    - role: homepage
