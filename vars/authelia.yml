---
config_file: "configuration.yml"

smtp:
  enabled: true
  host: ""
  user: ""
  password: ""
  sender: ""

secrets:
  jwt: "" # random alphanumeric string with 64+ characters
  session: "" # random alphanumeric string with 64+ characters
  storage_encryption_key: "" # random alphanumeric string with 64+ characters
  hmac: "" # random alphanumeric string with 64+ characters
  oidc_private: "" # private RSA key, generate keypair first
