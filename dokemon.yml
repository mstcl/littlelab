---
- name: Install dokemon
  hosts: homelab
  gather_facts: false
  vars:
    service_directory: "dokemon"
    service_image: "productiveops/dokemon"
    image_tag: "latest"
    url: "{{ urls.dokemon }}"

  roles:
    - role: urls
    - role: config
      tags: config
      vars:
        directories:
          - path: "{{ location.docker_data }}/{{ service_directory }}"
            owner: "1000"
            permission: "0755"
    - role: docker
      tags: docker
    - role: traefik
      tags: traefik
      vars:
        servers:
          - port: 9090
        middlewares:
          - "internal-ip@file"
