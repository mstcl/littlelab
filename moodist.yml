---
- name: Install moodist
  hosts: homelab
  gather_facts: false
  vars:
    service_directory: "moodist"
    service_image: "ghcr.io/remvze/moodist"
    image_tag: "latest"
    url: "{{ urls.moodist }}"

  roles:
    - role: urls
    - role: config
    - role: docker
    - role: traefik
      vars:
        servers:
          - port: 8080
